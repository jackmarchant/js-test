/**
 * Number to words is about translating the numerical digits into words.
 * Write a method/function that takes an integer and print the number in words.
 */

const n2w = (num) => {
  // Start here
}

describe("Number to Words", () => {
  test("1 converts to 'one'", () => {
    expect(n2w(1)).toEqual('one');
  })
});
